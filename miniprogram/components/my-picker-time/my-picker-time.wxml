<view>
  <van-datetime-picker
  type="date"
  title="选择年月日"
  value="{{ currentDate }}"
  bind:input="onInput"
  bind:confirm="confirm"
  bind:cancel="cancel"
  min-date="{{ minDate }}"
  formatter="{{ formatter }}"
/>
</view>