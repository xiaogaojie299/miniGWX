<view>
  <van-datetime-picker
  type="date"
  title="选择年月日"
  min-date="{{minDate}}"
  max-date="{{ maxDate }}"
  value="{{ currentDate }}" 
  bind:input="onInput"
  bind:confirm="confirm"
  bind:cancel="cancel"
  formatter="{{ formatter }}"
/>
  <!-- min-date="{{ minDate }}" -->
</view>