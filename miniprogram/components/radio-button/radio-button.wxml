<view class="radio-box">
        <view class="readio" data-index="{{1}}" catchtap="checkoutBtn">
          <!-- incon -->
          <image
            src="{{currentActive==1?selected:unselect}}"
            class="incon"
            alt=""
          />
          <view><slot name="radio1"></slot></view>
        </view>

        <view class="readio ml-30" data-index="{{2}}" catchtap="checkoutBtn">
          <!-- incon -->
          <image
            src="{{currentActive==2?selected:unselect}}"
            class="incon"
            alt=""
          />
          <view><slot name="radio2"> </slot></view>
        </view>
      </view>